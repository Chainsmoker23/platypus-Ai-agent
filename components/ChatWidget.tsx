import React from 'react';

const ChatWidget: React.FC = () => {
  // The chat widget functionality has been temporarily disabled to resolve a loading issue.
  return null;
};

export default ChatWidget;
